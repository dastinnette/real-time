<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.3/css/materialize.min.css'>
    <meta charset="utf-8">
    <title>crowdsource</title>
  </head>
  <body>

    <div class="container">
      <h1>
        <a href="https://www.youtube.com/watch?v=pWdd6_ZxX8c" target="_blank">
          Yeah, well, that's just, like, your opinion, man
        </a>
      </h1>
      <hr>

      <h4>Share this url and crowdsource the masses</h4>
      <p><%= url %></p>
      <h4>Poll results</h4>
      <h5><%= question %></h5>
      <div id="results">
        <% for (var option in results) { %>
          <p><%= option %>: <%= results[option] %></p>
        <% } %>
      </div>

      <h4>Poll status</h4>
      <div id="poll-status">
        <% if (closed) { %>
          <h5>--> Closed</h5>
        <% } else { %>
          <h5>--> Open</h5>
          <button class="btn" id="close-poll">Close poll</button>
        <% } %>
      </div>
      <br>
      <button class="btn"><a href="/" class="white-text">Create another poll</a></button>
      <div id="connection-count"></div>
    </div>

    <script src="https://code.jquery.com/jquery-2.2.0.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/client.js"></script>
  </body>
</html>
